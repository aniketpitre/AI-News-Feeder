apiVersion: v1
kind: Service

metadata:
  name: ai-news-feeder
  namespace: ai-news

spec:
  selector:
    app: ai-news-feeder

  ports:
  - name: http
    port: 80
    targetPort: 3000

  type: LoadBalancer
